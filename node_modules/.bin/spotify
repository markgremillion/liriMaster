#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../spotify-search/bin/spotify" "$@"
  ret=$?
else 
  node  "$basedir/../spotify-search/bin/spotify" "$@"
  ret=$?
fi
exit $ret
